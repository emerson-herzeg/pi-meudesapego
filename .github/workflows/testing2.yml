name: Deploy via ftp
on: 
 pull_request:
   branches:
   - testing
   types: [closed]
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@releases/v2
      with:
        host: ftp.meudesapego.tech
        user: u189699150.teste
        password: pi3@desapegoTeste
        localDir: "."
        remoteDir: "/home/u189699150/domains/meudesapego.tech/public_html/teste/"
        options: "--delete --asci"
